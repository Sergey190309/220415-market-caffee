import React from 'react';
import { Button } from 'semantic-ui-react';

const SignInOut = () => {
  return (
    <Button
      basic
    >
      Sign In
    </Button>
  );
};

export default SignInOut;
